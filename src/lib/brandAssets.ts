/**
 * Brand assets are served from `public/brand/`.
 *
 * They previously lived under `/lovable-uploads/`, which shipped that path into the rendered page
 * as the hero logo's `src`. Anyone reading the page source saw the generator's name and reported
 * the site as still Lovable-branded.
 */

/** Small header/button logo: the legal PNG mark. */
export const IEP_LOGO_COLORED = "/brand/iep-logo.png";

/** Large homepage hero logo: the transparent SVG version. */
export const IEP_LOGO_HERO = "/brand/iep-logo-welcome.svg";
